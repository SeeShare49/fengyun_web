<?php

namespace app\admin\validate;

use think\Validate;

class Preheatsource extends Validate
{
    /**
     * 定义验证规则
     * 格式：'字段名'    =>    ['规则1','规则2'...]
     *
     * @var array
     */
    protected $rule = [
        'source_url' => 'require',
    ];

    /**
     * 定义错误信息
     * 格式：'字段名.规则名'    =>    '错误信息'
     *
     * @var array
     */
    protected $message = [
        'require.source_url' => '请填写预热资源名称或地址',
    ];
}
